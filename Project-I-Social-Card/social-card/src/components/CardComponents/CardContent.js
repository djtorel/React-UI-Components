import React from 'react';
import './Card.css';

const CardContent = () => {
  return (
    <div>
      <div>Get started with React</div>
      <div>
        React makes it painless to create interactive UIs. Design simple views
        for each state in your application.
      </div>
      <div>reactjs.org</div>
    </div>
  );
};

export default CardContent;
